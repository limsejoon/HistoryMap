# 지도 웹페이지 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Leaflet + OpenStreetMap으로 마커 추가/삭제 가능한 지도 웹페이지를 단일 HTML 파일로 구현

**Architecture:** 단일 `map.html` 파일에 HTML/CSS/JS 전부 포함. Leaflet.js CDN으로 로드, OpenStreetMap 타일 사용. 마커는 Leaflet 레이어로만 관리, 별도 JS 상태 없음.

**Tech Stack:** Vanilla HTML5, CSS3, Vanilla JS, Leaflet.js 1.9.x (CDN), OpenStreetMap 타일

---

## 파일 구조

- **Create:** `C:/dev/HistoryMap/map.html` — 지도 앱 전체 (구조 + 스타일 + 로직)

---

### Task 1: HTML 뼈대 + Leaflet 로드 + 지도 초기화

**Files:**
- Create: `C:/dev/HistoryMap/map.html`

- [ ] **Step 1: map.html 전체 작성**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HistoryMap</title>
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body { height: 100%; }
    #map { height: 100vh; width: 100vw; }
    .hint {
      position: fixed;
      bottom: 24px;
      left: 50%;
      transform: translateX(-50%);
      background: rgba(0, 0, 0, 0.6);
      color: white;
      padding: 6px 16px;
      border-radius: 20px;
      font-size: 0.85em;
      pointer-events: none;
      z-index: 1000;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }
  </style>
</head>
<body>
  <div id="map"></div>
  <div class="hint">지도를 클릭하면 마커가 찍힙니다</div>

  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script>
    const map = L.map('map').setView([20, 0], 2);

    L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
      maxZoom: 19,
      attribution: '© <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);
  </script>
</body>
</html>
```

- [ ] **Step 2: 브라우저에서 확인**

`C:/dev/HistoryMap/map.html`을 브라우저에서 열어 확인:
- 세계지도가 화면 전체를 채움
- 줌 컨트롤(+/−)이 좌상단에 표시됨
- 하단에 "지도를 클릭하면 마커가 찍힙니다" 힌트 문구 표시
- 지도 우하단에 OpenStreetMap 저작권 표시

- [ ] **Step 3: 커밋**

```bash
git -C C:/dev/HistoryMap add map.html
git -C C:/dev/HistoryMap commit -m "feat: add map with Leaflet and OSM tiles"
```

---

### Task 2: 클릭 → 마커 추가

**Files:**
- Modify: `C:/dev/HistoryMap/map.html` — `<script>` 블록 (tileLayer 추가 코드 아래)

- [ ] **Step 1: map 클릭 핸들러 추가**

`L.tileLayer(...).addTo(map);` 바로 아래에 추가:

```js
    map.on('click', function (e) {
      const { lat, lng } = e.latlng;
      const marker = L.marker([lat, lng]).addTo(map);
      marker.openPopup();
    });
```

- [ ] **Step 2: 브라우저에서 확인**

1. 페이지 새로고침
2. 지도 아무 곳이나 클릭 → 파란 Leaflet 기본 마커가 찍힘
3. 여러 번 클릭 → 여러 개 마커 추가됨

콘솔 검증 (F12):
```js
// 클릭 후 콘솔에서 map의 레이어 수 확인
let count = 0;
map.eachLayer(() => count++);
console.assert(count > 1, 'markers must have been added'); // 타일 레이어(1) + 마커들
```

- [ ] **Step 3: 커밋**

```bash
git -C C:/dev/HistoryMap add map.html
git -C C:/dev/HistoryMap commit -m "feat: add marker on map click"
```

---

### Task 3: 팝업 — 좌표 표시 + 삭제 버튼

**Files:**
- Modify: `C:/dev/HistoryMap/map.html` — Task 2에서 추가한 `map.on('click', ...)` 핸들러 교체

- [ ] **Step 1: 클릭 핸들러를 팝업 포함 버전으로 교체**

기존:
```js
    map.on('click', function (e) {
      const { lat, lng } = e.latlng;
      const marker = L.marker([lat, lng]).addTo(map);
      marker.openPopup();
    });
```

교체 후:
```js
    function formatCoord(lat, lng) {
      const latStr = lat >= 0
        ? lat.toFixed(4) + '° N'
        : Math.abs(lat).toFixed(4) + '° S';
      const lngStr = lng >= 0
        ? lng.toFixed(4) + '° E'
        : Math.abs(lng).toFixed(4) + '° W';
      return latStr + ', ' + lngStr;
    }

    map.on('click', function (e) {
      const { lat, lng } = e.latlng;
      const marker = L.marker([lat, lng]).addTo(map);

      marker.bindPopup(
        '<div style="text-align:center;font-family:sans-serif;min-width:160px">' +
        '<p style="margin:0 0 8px;font-weight:600">' + formatCoord(lat, lng) + '</p>' +
        '<button class="delete-btn" style="background:#e53935;color:white;border:none;' +
        'border-radius:4px;padding:5px 14px;cursor:pointer;font-size:0.9em">🗑 삭제</button>' +
        '</div>'
      );

      marker.on('popupopen', function (ev) {
        ev.popup.getElement()
          .querySelector('.delete-btn')
          .addEventListener('click', function () {
            map.removeLayer(marker);
          });
      });

      marker.openPopup();
    });
```

- [ ] **Step 2: 브라우저에서 전체 흐름 검증**

1. 지도 클릭 → 마커 추가 + 팝업 자동 오픈
2. 팝업에 좌표가 표시됨 (예: `37.5665° N, 126.9780° E`)
3. 남반구/서반구 클릭 시 S/W 표기 확인 (남미, 아프리카 등)
4. "🗑 삭제" 버튼 클릭 → 마커가 지도에서 제거됨
5. 마커 없는 곳 클릭 → 새 마커 정상 추가됨

- [ ] **Step 3: 커밋**

```bash
git -C C:/dev/HistoryMap add map.html
git -C C:/dev/HistoryMap commit -m "feat: add popup with coordinates and delete button"
```

---

## 자체 검토 (Spec Coverage)

| 스펙 항목 | 구현 Task |
|-----------|-----------|
| Leaflet.js CDN 로드 | Task 1 |
| OpenStreetMap 타일 | Task 1 (`tile.openstreetmap.org`) |
| 초기 뷰: 세계 전체 (zoom 2) | Task 1 (`setView([20, 0], 2)`) |
| 지도 클릭 → 마커 추가 | Task 2, 3 |
| 팝업: 위도/경도 자동 표시 | Task 3 (`formatCoord`) |
| N/S/E/W 방향 표기 | Task 3 (`formatCoord` 조건문) |
| 팝업 삭제 버튼 | Task 3 (`delete-btn`) |
| 삭제 버튼 → 마커 제거 | Task 3 (`map.removeLayer`) |
| 단일 HTML 파일 | 전 Task |
| API 키 불필요 | Task 1 (OSM 무료 타일) |
| 안내 문구 | Task 1 (`.hint`) |
| 저작권 표시 | Task 1 (attribution) |
